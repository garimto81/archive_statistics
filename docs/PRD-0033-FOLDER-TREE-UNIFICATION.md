# PRD-0033: 마스터 폴더 트리 컴포넌트 설계

**Version**: 2.0.0
**Created**: 2025-12-12
**Status**: Draft
**Priority**: Medium

---

## 1. 설계 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                     설계 방식                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 마스터 폴더 트리구조 설계                                     │
│     └── 모든 기능을 포함한 단일 컴포넌트                          │
│                                                                 │
│  2. 커스터마이징 옵션 추가                                        │
│     └── Props로 기능 ON/OFF 제어                                 │
│                                                                 │
│  3. 탭별 요구사항에 맞게 커스터마이징                              │
│     └── 프리셋 Props 조합으로 각 페이지 구현                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 탭별 요구사항 분석

### 2.1 Dashboard (Progress View)

| 기능 | 필요 여부 | 설명 |
|------|:---------:|------|
| 진행률 바 | ✅ | work_summary 기반 Gantt-chart 스타일 |
| 작업 현황 배지 | ✅ | done/total 표시 |
| 파일 표시 | ✅ | 폴더 내 파일 목록 |
| 확장자 필터 | ✅ | 외부 ExtensionFilter 연동 |
| 숨김 파일 필터 | ✅ | include_hidden 옵션 |
| Lazy Loading | ✅ | 폴더 클릭 시 동적 로드 |
| 자동 갱신 | ✅ | 60초 주기 |
| 상세 패널 | ✅ | FolderProgressDetail (우측) |
| 코덱 정보 | ❌ | 불필요 |
| 검색 | ❌ | 불필요 |

### 2.2 Folders (Explorer View)

| 기능 | 필요 여부 | 설명 |
|------|:---------:|------|
| 진행률 바 | ❌ | 기본 탐색 목적 |
| 작업 현황 배지 | ❌ | 불필요 |
| 파일 표시 | ❌ | 폴더만 표시 |
| 확장자 필터 | ⚪ | 옵션 (추후 추가 가능) |
| 숨김 파일 필터 | ⚪ | 옵션 |
| Lazy Loading | ✅ | 성능 개선 |
| 자동 갱신 | ❌ | 수동 새로고침 |
| 상세 패널 | ✅ | 파일 타입 분포 (우측) |
| 코덱 정보 | ❌ | 불필요 |
| 검색 | ✅ | 폴더 검색 |

### 2.3 Statistics - Codec Explorer (Codec View)

| 기능 | 필요 여부 | 설명 |
|------|:---------:|------|
| 진행률 바 | ❌ | 코덱 정보 중심 |
| 작업 현황 배지 | ❌ | 불필요 |
| 파일 표시 | ❌ | 폴더만 표시 |
| 확장자 필터 | ❌ | 불필요 |
| 숨김 파일 필터 | ❌ | 불필요 |
| Lazy Loading | ✅ | 성능 개선 |
| 자동 갱신 | ❌ | 불필요 |
| 상세 패널 | ✅ | CodecFolderDetail (우측) |
| 코덱 정보 | ✅ | 코덱 배지 표시 |
| 검색 | ❌ | 불필요 |

---

## 3. 마스터 컴포넌트 설계

### 3.1 MasterFolderTree Props Interface

```typescript
/**
 * MasterFolderTree - 통합 폴더 트리 컴포넌트
 *
 * 모든 폴더 트리 기능을 포함하며, Props로 필요한 기능만 활성화
 */
interface MasterFolderTreeProps {
  // ═══════════════════════════════════════════════════════════
  // 1. 데이터 옵션
  // ═══════════════════════════════════════════════════════════
  initialPath?: string;           // 시작 경로 (기본: 루트)
  initialDepth?: number;          // 초기 로드 깊이 (기본: 2)

  // ═══════════════════════════════════════════════════════════
  // 2. 표시 옵션 (Display)
  // ═══════════════════════════════════════════════════════════
  showFiles?: boolean;            // 파일 목록 표시 (기본: false)
  showProgressBar?: boolean;      // 진행률 바 표시 (기본: false)
  showWorkBadge?: boolean;        // 작업 현황 배지 표시 (기본: false)
  showCodecBadge?: boolean;       // 코덱 배지 표시 (기본: false)
  showSizeInfo?: boolean;         // 용량 정보 표시 (기본: true)
  showFileCount?: boolean;        // 파일 수 표시 (기본: true)

  // ═══════════════════════════════════════════════════════════
  // 3. 필터 옵션 (Filter)
  // ═══════════════════════════════════════════════════════════
  selectedExtensions?: string[];  // 확장자 필터 (외부 연동)
  showHiddenFiles?: boolean;      // 숨김 파일 포함 (기본: false)
  searchQuery?: string;           // 검색어 (폴더명 필터링)

  // ═══════════════════════════════════════════════════════════
  // 4. 동작 옵션 (Behavior)
  // ═══════════════════════════════════════════════════════════
  enableLazyLoading?: boolean;    // Lazy Loading 활성화 (기본: true)
  enableAutoRefresh?: boolean;    // 자동 갱신 활성화 (기본: false)
  autoRefreshInterval?: number;   // 갱신 주기 (ms, 기본: 60000)

  // ═══════════════════════════════════════════════════════════
  // 5. 상세 패널 옵션 (Detail Panel)
  // ═══════════════════════════════════════════════════════════
  detailPanelMode?: 'none' | 'progress' | 'codec' | 'explorer';
  // - none: 상세 패널 없음 (외부에서 처리)
  // - progress: 진행률 상세 (Dashboard)
  // - codec: 코덱 상세 (Statistics)
  // - explorer: 파일 타입 분포 (Folders)

  // ═══════════════════════════════════════════════════════════
  // 6. 콜백 (Callbacks)
  // ═══════════════════════════════════════════════════════════
  onFolderSelect?: (folder: FolderWithProgress) => void;
  onFileSelect?: (file: FileNode) => void;
  onFolderExpand?: (folder: FolderWithProgress) => void;

  // ═══════════════════════════════════════════════════════════
  // 7. 스타일 옵션 (Style)
  // ═══════════════════════════════════════════════════════════
  className?: string;             // 추가 CSS 클래스
  height?: string | number;       // 컴포넌트 높이 (기본: auto)
  compact?: boolean;              // 컴팩트 모드 (기본: false)
}
```

### 3.2 옵션 조합표

| 옵션 | Dashboard | Folders | Statistics |
|------|:---------:|:-------:|:----------:|
| **showFiles** | ✅ true | ❌ false | ❌ false |
| **showProgressBar** | ✅ true | ❌ false | ❌ false |
| **showWorkBadge** | ✅ true | ❌ false | ❌ false |
| **showCodecBadge** | ❌ false | ❌ false | ✅ true |
| **showSizeInfo** | ✅ true | ✅ true | ✅ true |
| **showFileCount** | ✅ true | ✅ true | ❌ false |
| **selectedExtensions** | ✅ 외부연동 | ⚪ 옵션 | ❌ null |
| **showHiddenFiles** | ✅ 외부연동 | ⚪ 옵션 | ❌ false |
| **searchQuery** | ❌ null | ✅ 내부상태 | ❌ null |
| **enableLazyLoading** | ✅ true | ✅ true | ✅ true |
| **enableAutoRefresh** | ✅ true | ❌ false | ❌ false |
| **detailPanelMode** | 'progress' | 'explorer' | 'codec' |

---

## 4. 탭별 프리셋 구현

### 4.1 Dashboard 프리셋

```typescript
// pages/Dashboard.tsx
<MasterFolderTree
  // 데이터
  initialDepth={3}

  // 표시
  showFiles={true}
  showProgressBar={true}
  showWorkBadge={true}
  showCodecBadge={false}

  // 필터 (외부 상태 연동)
  selectedExtensions={extensionsArray}
  showHiddenFiles={showHiddenFiles}

  // 동작
  enableLazyLoading={true}
  enableAutoRefresh={true}
  autoRefreshInterval={60000}

  // 상세 패널
  detailPanelMode="progress"

  // 콜백
  onFolderSelect={handleFolderSelect}
  onFileSelect={handleFileSelect}

  // 스타일
  className="h-[calc(100vh-200px)]"
/>
```

### 4.2 Folders 프리셋

```typescript
// pages/Folders.tsx
<MasterFolderTree
  // 데이터
  initialDepth={2}

  // 표시
  showFiles={false}
  showProgressBar={false}
  showWorkBadge={false}
  showCodecBadge={false}
  showSizeInfo={true}
  showFileCount={true}

  // 필터
  searchQuery={searchQuery}
  showHiddenFiles={false}

  // 동작
  enableLazyLoading={true}
  enableAutoRefresh={false}

  // 상세 패널
  detailPanelMode="explorer"

  // 콜백
  onFolderSelect={handleFolderSelect}

  // 스타일
  height={600}
/>
```

### 4.3 Statistics (Codec Explorer) 프리셋

```typescript
// pages/Statistics.tsx (Codec Explorer 탭)
<MasterFolderTree
  // 데이터
  initialDepth={2}

  // 표시
  showFiles={false}
  showProgressBar={false}
  showWorkBadge={false}
  showCodecBadge={true}
  showSizeInfo={true}
  showFileCount={false}

  // 필터
  showHiddenFiles={false}

  // 동작
  enableLazyLoading={true}
  enableAutoRefresh={false}

  // 상세 패널
  detailPanelMode="codec"

  // 콜백
  onFolderSelect={handleFolderSelect}

  // 스타일
  height={600}
/>
```

---

## 5. 컴팩트 필터바 설계

### 5.1 현재 문제점

```
┌─────────────────────────────────────────────────────────────────┐
│ 현재: 필터 패널이 별도 영역 차지 (2-3줄)                          │
├─────────────────────────────────────────────────────────────────┤
│ ┌─ Extension Filter ──────────────────────────────────────────┐ │
│ │ [Video] [Audio] [Image]  👁 Hidden                          │ │
│ │ [mp4] [mkv] [mov] [avi] [mp3] [wav] ...                     │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌─ Folder Tree ───────────────────────────────────────────────┐ │
│ │ ...                                                         │ │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 목표 UI: 한 줄 컴팩트 필터바

```
┌─────────────────────────────────────────────────────────────────┐
│ 목표: 제목 + 필터를 한 줄에 통합                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ Progress Overview  [📁 Ext ▼] [👁 Hidden] [📊 Progress] [🎬 Codec] │
│ ├── WSOP (1,335 files)  ████████████░░░░ 78%                   │
│ ├── PAD (44 files)      ██████░░░░░░░░░░ 42%                   │
│ └── ...                                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 CompactFilterBar 컴포넌트

```typescript
/**
 * CompactFilterBar - 한 줄 필터바
 *
 * 제목과 필터 옵션을 한 줄에 배치하여 공간 절약
 */
interface CompactFilterBarProps {
  title: string;                    // "Progress Overview", "Folders", etc.

  // 필터 옵션 (드롭다운/토글)
  filters: {
    // 확장자 필터 (드롭다운)
    extensions?: {
      enabled: boolean;
      selected: string[];
      available: string[];
      onChange: (exts: string[]) => void;
    };

    // 숨김 파일 (토글)
    hidden?: {
      enabled: boolean;
      show: boolean;
      onChange: (show: boolean) => void;
    };

    // 표시 옵션 (토글 그룹)
    display?: {
      showProgress?: boolean;       // 진행률 바
      showCodec?: boolean;          // 코덱 배지
      showFiles?: boolean;          // 파일 표시
      onChange: (key: string, value: boolean) => void;
    };
  };

  // 우측 액션
  actions?: React.ReactNode;        // Refresh 버튼 등
}
```

### 5.4 필터 드롭다운 UI

```
┌──────────────────────────────────────────────────────────────────┐
│  [📁 Extensions ▼]  클릭 시 드롭다운                              │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────┐                          │
│  │ Quick Select                       │                          │
│  │ ○ All  ● Video  ○ Audio  ○ Image  │                          │
│  ├────────────────────────────────────┤                          │
│  │ ☑ mp4 (892)    ☑ mkv (234)        │                          │
│  │ ☑ mov (156)    ☐ avi (23)         │                          │
│  │ ☐ mp3 (12)     ☐ wav (8)          │                          │
│  ├────────────────────────────────────┤                          │
│  │ [Clear] [Apply]                    │                          │
│  └────────────────────────────────────┘                          │
└──────────────────────────────────────────────────────────────────┘
```

### 5.5 표시 옵션 토글

| 토글 | 아이콘 | 기능 | 적용 대상 |
|------|--------|------|----------|
| Progress | 📊 | 진행률 바 ON/OFF | Dashboard |
| Codec | 🎬 | 코덱 배지 ON/OFF | Statistics |
| Files | 📄 | 파일 목록 ON/OFF | Dashboard |
| Hidden | 👁 | 숨김 파일 포함 | All |

### 5.6 페이지별 필터바 구성

```typescript
// Dashboard - 모든 필터 활성화
<CompactFilterBar
  title="Progress Overview"
  filters={{
    extensions: { enabled: true, ... },
    hidden: { enabled: true, ... },
    display: {
      showProgress: true,    // 토글 가능
      showFiles: true,       // 토글 가능
    }
  }}
  actions={<RefreshButton />}
/>

// Folders - 확장자 + 검색
<CompactFilterBar
  title="Folder Explorer"
  filters={{
    extensions: { enabled: true, ... },
    hidden: { enabled: true, ... },
  }}
  actions={<SearchInput />}
/>

// Statistics (Codec Explorer) - 코덱 토글만
<CompactFilterBar
  title="Codec Explorer"
  filters={{
    display: {
      showCodec: true,       // 항상 ON
    }
  }}
/>
```

---

## 6. 상세 패널 통합 설계

### 6.1 MasterFolderDetail Props Interface

```typescript
/**
 * MasterFolderDetail - 통합 폴더 상세 패널
 */
interface MasterFolderDetailProps {
  folder: FolderWithProgress | null;
  mode: 'progress' | 'codec' | 'explorer';
  onClose?: () => void;
}
```

### 6.2 Mode별 렌더링 섹션

```typescript
function MasterFolderDetail({ folder, mode, onClose }: MasterFolderDetailProps) {
  if (!folder) return <EmptyState mode={mode} />;

  return (
    <div className="folder-detail-panel">
      {/* 공통: 헤더 (폴더명, 경로, 닫기 버튼) */}
      <DetailHeader folder={folder} onClose={onClose} />

      {/* 공통: 기본 통계 (용량, 파일 수, 폴더 수) */}
      <BasicStats folder={folder} />

      {/* Mode별 조건부 렌더링 */}
      {mode === 'progress' && (
        <>
          <ProgressSection folder={folder} />      {/* 진행률 상세 */}
          <WorkStatusList folder={folder} />       {/* Google Sheets 원본 */}
          <FilesList folder={folder} />            {/* 파일 목록 */}
        </>
      )}

      {mode === 'codec' && (
        <>
          <CodecDistribution folder={folder} />    {/* 코덱 분포 차트 */}
          <CodecFilesList folder={folder} />       {/* 코덱별 파일 목록 */}
        </>
      )}

      {mode === 'explorer' && (
        <>
          <FileTypeDistribution folder={folder} /> {/* 파일 타입 분포 */}
          <SubfolderList folder={folder} />        {/* 하위 폴더 목록 */}
        </>
      )}
    </div>
  );
}
```

---

## 7. 컴포넌트 구조

### 7.1 파일 구조

```
frontend/src/components/
├── MasterFolderTree/
│   ├── index.tsx                 # 메인 export
│   ├── MasterFolderTree.tsx      # 마스터 트리 컴포넌트
│   ├── FolderNode.tsx            # 폴더 노드 (재귀)
│   ├── FileNode.tsx              # 파일 노드
│   ├── ProgressBar.tsx           # 진행률 바
│   ├── CodecBadge.tsx            # 코덱 배지
│   ├── WorkBadge.tsx             # 작업 현황 배지
│   ├── SearchInput.tsx           # 검색 입력
│   ├── Legend.tsx                # 범례
│   └── hooks/
│       ├── useFolderTree.ts      # 트리 데이터 훅
│       ├── useLazyLoading.ts     # Lazy Loading 훅
│       └── useAutoRefresh.ts     # 자동 갱신 훅
│
├── MasterFolderDetail/
│   ├── index.tsx                 # 메인 export
│   ├── MasterFolderDetail.tsx    # 마스터 상세 패널
│   ├── DetailHeader.tsx          # 공통 헤더
│   ├── BasicStats.tsx            # 기본 통계
│   ├── sections/
│   │   ├── ProgressSection.tsx   # 진행률 섹션
│   │   ├── CodecSection.tsx      # 코덱 섹션
│   │   └── ExplorerSection.tsx   # 탐색 섹션
│   └── EmptyState.tsx            # 빈 상태
│
└── [삭제 예정]
    ├── FolderTree.tsx            # Phase 2에서 삭제
    ├── FolderTreeWithProgress.tsx # Phase 3에서 삭제
    └── CodecFolderDetail.tsx     # Phase 2에서 삭제
```

### 7.2 컴포넌트 의존성

```
┌─────────────────────────────────────────────────────────────────┐
│                    컴포넌트 의존성 트리                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  MasterFolderTree                                               │
│  ├── FolderNode (재귀)                                          │
│  │   ├── ProgressBar      [showProgressBar=true]                │
│  │   ├── WorkBadge        [showWorkBadge=true]                  │
│  │   ├── CodecBadge       [showCodecBadge=true]                 │
│  │   └── FileNode         [showFiles=true]                      │
│  ├── SearchInput          [searchQuery 사용 시]                  │
│  ├── Legend               [showProgressBar=true]                │
│  └── MasterFolderDetail   [detailPanelMode !== 'none']          │
│      ├── DetailHeader                                           │
│      ├── BasicStats                                             │
│      ├── ProgressSection  [mode='progress']                     │
│      ├── CodecSection     [mode='codec']                        │
│      └── ExplorerSection  [mode='explorer']                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. 마이그레이션 계획

### 8.1 Phase 1: 마스터 컴포넌트 생성 (신규)

| # | 작업 | 산출물 | 예상 시간 |
|---|------|--------|----------|
| 1.1 | MasterFolderTree 디렉토리 구조 생성 | 디렉토리 | 5분 |
| 1.2 | MasterFolderTree.tsx 기본 구조 작성 | 컴포넌트 | 1시간 |
| 1.3 | FolderNode, FileNode 컴포넌트 분리 | 컴포넌트 | 30분 |
| 1.4 | ProgressBar, WorkBadge, CodecBadge 분리 | 컴포넌트 | 30분 |
| 1.5 | useFolderTree, useLazyLoading 훅 작성 | 훅 | 45분 |
| 1.6 | MasterFolderDetail 컴포넌트 작성 | 컴포넌트 | 1시간 |

**총 예상 시간**: 4시간

### 8.2 Phase 2: 페이지 마이그레이션

| # | 작업 | 대상 | 예상 시간 |
|---|------|------|----------|
| 2.1 | Dashboard.tsx 마이그레이션 | Dashboard | 30분 |
| 2.2 | Folders.tsx 마이그레이션 | Folders | 45분 |
| 2.3 | Statistics.tsx 마이그레이션 | Statistics | 30분 |
| 2.4 | 통합 테스트 | 전체 | 30분 |

**총 예상 시간**: 2.5시간

### 8.3 Phase 3: 레거시 정리

| # | 작업 | 대상 | 예상 시간 |
|---|------|------|----------|
| 3.1 | FolderTree.tsx 삭제 | 레거시 | 5분 |
| 3.2 | FolderTreeWithProgress.tsx 삭제 | 레거시 | 5분 |
| 3.3 | CodecFolderDetail.tsx 삭제 | 레거시 | 5분 |
| 3.4 | foldersApi 제거 (api.ts) | API | 10분 |
| 3.5 | 미사용 타입 정리 | types | 10분 |
| 3.6 | 최종 검증 | 전체 | 20분 |

**총 예상 시간**: 1시간

---

## 9. API 통합

### 9.1 단일 API 사용

```typescript
// services/api.ts

// 기존 (중복)
export const foldersApi = { ... }    // 제거 예정
export const progressApi = { ... }   // 유지

// 통합 후
export const progressApi = {
  // 트리 데이터 조회 (모든 옵션 지원)
  getTreeWithProgress: (options: TreeOptions) => Promise<TreeResponse>,

  // 폴더 상세 조회
  getFolderDetail: (path: string, options: DetailOptions) => Promise<FolderDetail>,

  // 진행률 요약
  getSummary: (options: SummaryOptions) => Promise<ProgressSummary>,
};

interface TreeOptions {
  path?: string;
  depth?: number;
  showFiles?: boolean;
  extensions?: string[];
  includeCodecs?: boolean;
  includeHidden?: boolean;
}
```

---

## 10. 성공 기준

### 10.1 기능 요구사항

- [ ] Dashboard: 기존 모든 기능 유지 (진행률, 필터, 파일 표시)
- [ ] Folders: Lazy Loading 추가, 기존 탐색 기능 유지
- [ ] Statistics: Codec Explorer 기존 기능 유지

### 10.2 코드 품질 요구사항

- [ ] 단일 마스터 컴포넌트로 통합
- [ ] 레거시 컴포넌트 3개 삭제 완료
- [ ] Props 기반 커스터마이징 구현

### 10.3 성능 요구사항

- [ ] 번들 사이즈 증가 없음 (레거시 삭제로 상쇄)
- [ ] 각 페이지 초기 로드 시간 유지

---

## 11. 위험 요소 및 대응

| 위험 | 영향도 | 대응 방안 |
|------|--------|----------|
| 컴포넌트 복잡도 증가 | Medium | 서브 컴포넌트로 분리, 훅으로 로직 추출 |
| 마이그레이션 중 기능 손실 | High | Phase별 독립 테스트, 롤백 계획 준비 |
| 성능 저하 | Medium | Tree shaking 확인, 불필요 렌더링 최적화 |

---

## 12. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 2.1.0 | 2025-12-12 | 컴팩트 필터바 설계 추가 (Section 5) |
| 2.0.0 | 2025-12-12 | 마스터 컴포넌트 + 옵션 기반으로 재설계 |
| 1.0.0 | 2025-12-12 | 초안 작성 |
