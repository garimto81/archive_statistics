# Google Sheets 통합 솔루션 설계서

**작성일**: 2025-12-09
**버전**: 2.0
**상태**: Implemented
**최종 수정**: 2025-12-09

---

## 1. 개요

Archive Statistics Dashboard에서 3개의 Google Sheets를 각각의 목적에 맞게 동기화하여 활용합니다.

### 1.1 시트 현황

| # | Sheet 이름         | URL           | 용도                 | 구현 상태   |
|---|--------------------|---------------|----------------------|-------------|
| 1 | Work Status        | `1xuN4_...`   | 작업 현황 추적       | ✅ 구현 완료 |
| 2 | WSOP Hand Database | `1pUMPKe-...` | 핸드 메타데이터      | ❌ 미구현    |
| 3 | WSOP Circuit LA    | `1_RN_W_Z...` | 핸드 분석 (타임코드) | ❌ 미구현    |

---

## 2. Sheet 1: Work Status (✅ 구현 완료)

### 2.1 시트 구조

```
워크시트: Sheet1
행 수: 13 (헤더 2행 + 데이터 11행)
헤더 위치: Row 2
```

| 컬럼 | 설명 | 예시 |
|------|------|------|
| Archive | 아카이브명 (병합 셀) | WSOP, HCL |
| Category | 카테고리/이벤트명 | 2023 WSOP Paradise |
| PIC | 담당자 | Richie Hunter |
| Status | 상태 | 작업 중, 완료, 대기 |
| Total | 총 비디오 수 | 26 |
| Excel Done | 완료된 비디오 수 | 7 |
| Progress % | 진행률 (수식) | 26.92% |
| Notes 1/2 | 비고 | - |

### 2.2 현재 구현

- **서비스**: `backend/app/services/sheets_sync.py`
- **동기화 주기**: 30분
- **특수 처리**:
  - 병합 셀 → 이전 행 값 상속
  - Status 정규화 (작업 중, 진행 중, 작업 中 → "작업 중")
  - 빈 Category 스킵

### 2.3 추가 개선 사항 (Optional)

- [ ] 시트에서 삭제된 항목 자동 감지 및 DB 삭제
- [ ] Status 필드 유효성 검증 경고

---

## 3. Sheet 2: WSOP Hand Database (⏸️ 제외)

> **결정**: 이 시트는 현재 구현 범위에서 **제외**합니다.

### 3.1 제외 사유

| 사유 | 설명 |
|------|------|
| 타임코드 부재 | `time_start_ms`, `time_end_ms` 필드가 대부분 비어있음 |
| 진행률 추적 불가 | 영상 완료 판정에 필요한 데이터 없음 |
| 중복 데이터 | Sheet 3의 핸드 데이터와 일부 중복 |
| 별도 시스템용 | MAM(Media Asset Management) 시스템용 메타데이터 |

### 3.2 시트 정보 (참고용)

- **URL**: `https://docs.google.com/spreadsheets/d/1pUMPKe-OsKc-Xd8lH1cP9ctJO4hj3keXY5RwNFp2Mtk`
- **행 수**: 2,451개
- **주요 컬럼**: id, title, Description, ProjectName, Year_, Location

### 3.3 향후 활용 가능성

필요 시 별도 검색 시스템(MeiliSearch 연동)으로 구현 가능하나, 현재 우선순위에서 제외.

---

## 4. Sheet 3: WSOP Circuit LA (❌ 미구현)

### 4.1 시트 구조

```
총 8개 워크시트:
├── 2024 WSOPC LA (41행)
├── 2025 WSOPSC CYPRUS (33행)
├── WSOPE 2008-2013 (998행)
├── WSOPE 2024 (29행)
├── PAD S12,13 (112행)
├── 2023 WSOP Paradise (47행)
├── 2025 WSOP Las Vegas (62행)
└── MPP (35행)

헤더 위치: Row 1 또는 Row 3 (워크시트별 상이)
```

| 컬럼 | 설명 | 예시 |
|------|------|------|
| File No. | 핸드 순번 | 1, 2, 3... |
| File Name | 영상 파일명 | 2024 WSOP Circuit Los Angeles... |
| Nas Folder Link | NAS 경로 | \\\\10.10.100.122\\... |
| **In** | 시작 타임코드 | 6:58:55 |
| **Out** | 종료 타임코드 | 7:00:47 |
| Hand Grade | 핸드 등급 | ★, ★★, ★★★ |
| Winner | 우승자 | JJ |
| Hands | 핸드 조합 | 88 vs JJ |
| Tag (Player) | 플레이어 태그 | baby shark, ivey |
| Tag (Poker Play) | 플레이 태그 | bluff, fold |

### 4.2 핵심 가치

**타임코드 기반 진행률 추적 가능!**

- `In`, `Out` 필드에 정확한 타임코드 존재
- NAS 파일과 1:1 매칭 가능 (Nas Folder Link)
- 완료 판정 공식 적용 가능: `max(Out) ≥ 영상길이 × 0.9`

### 4.3 솔루션 설계

#### 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│              WSOP Circuit LA (8개 워크시트)                      │
│  - 핸드 타임코드 데이터                                          │
│  - File Name, In, Out, Hand Grade, Tags                         │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 동기화
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    hand_analysis 테이블                          │
│  - id (PK)                                                      │
│  - file_name, nas_path                                          │
│  - timecode_in_sec, timecode_out_sec                            │
│  - hand_grade, winner, hands                                    │
│  - player_tags (JSON), poker_play_tags (JSON)                   │
│  - source_worksheet, source_row                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 매칭
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    file_progress 테이블                          │
│  - nas_path (PK) ← NAS 스캔 결과                                │
│  - file_duration_sec ← ffprobe                                  │
│  - max_timecode_sec ← max(hand_analysis.timecode_out_sec)       │
│  - progress_rate = max_timecode / duration                      │
│  - completion_status = 'completed' if progress >= 0.9           │
└─────────────────────────────────────────────────────────────────┘
```

#### 데이터베이스 스키마

```sql
-- 핸드 분석 테이블
CREATE TABLE hand_analysis (
    id INTEGER PRIMARY KEY AUTOINCREMENT,

    -- 파일 정보
    file_name TEXT NOT NULL,
    nas_path TEXT,                      -- NAS 경로 (매칭용)

    -- 타임코드
    timecode_in TEXT,                   -- 원본 (HH:MM:SS)
    timecode_out TEXT,                  -- 원본 (HH:MM:SS)
    timecode_in_sec REAL,               -- 초 변환
    timecode_out_sec REAL,              -- 초 변환

    -- 핸드 정보
    hand_grade TEXT,                    -- ★, ★★, ★★★
    winner TEXT,
    hands TEXT,                         -- "88 vs JJ"

    -- 태그 (JSON)
    player_tags TEXT,                   -- ["baby shark", "ivey"]
    poker_play_tags TEXT,               -- ["bluff", "fold"]

    -- 출처
    source_worksheet TEXT,              -- "2024 WSOPC LA"
    source_row INTEGER,

    -- 메타
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_hand_file_name ON hand_analysis(file_name);
CREATE INDEX idx_hand_timecode ON hand_analysis(timecode_out_sec DESC);
```

#### 구현 계획

| 단계 | 작업 | 예상 시간 |
|------|------|----------|
| 1 | `hand_analysis` 모델 생성 | 0.5일 |
| 2 | HandAnalysisSyncService 구현 | 1.5일 |
|   | - 8개 워크시트 순회 | |
|   | - 헤더 위치 자동 감지 (Row 1 or Row 3) | |
|   | - 타임코드 파싱 (HH:MM:SS → 초) | |
| 3 | NAS 파일 매칭 로직 | 1일 |
|   | - File Name + Nas Folder Link → nas_path 추출 | |
|   | - file_progress 테이블과 조인 | |
| 4 | 진행률 계산 서비스 | 0.5일 |
|   | - max(timecode_out_sec) / file_duration | |
|   | - 90% 기준 완료 판정 | |
| 5 | API 엔드포인트 | 0.5일 |
|   | - GET /api/progress/summary | |
|   | - GET /api/progress/by-archive | |
|   | - GET /api/hands?file_name=... | |
| 6 | Frontend UI | 1일 |
|   | - Progress 페이지 추가 | |
|   | - 영상별 진행률 바 | |
|   | - 핸드 목록 표시 | |

**총 예상 시간: 5일**

---

## 5. 통합 아키텍처

### 5.1 서비스 구성

```
┌─────────────────────────────────────────────────────────────────┐
│                    SheetsSyncManager                             │
│  - 전체 동기화 오케스트레이션                                    │
│  - 동기화 순서: Work Status → Hand Analysis                      │
└─────────────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┴─────────────────┐
         ▼                                   ▼
┌─────────────────┐                 ┌─────────────────┐
│ WorkStatusSync  │                 │ HandAnalysisSync│
│ (✅ 구현 완료)   │                 │ (Sheet 3)       │
└─────────────────┘                 └─────────────────┘
         │                                   │
         ▼                                   ▼
┌─────────────────┐                 ┌─────────────────┐
│ work_status     │                 │ hand_analysis   │
│ 테이블          │                 │ 테이블          │
└─────────────────┘                 └─────────────────┘
```

### 5.2 설정 구조

```python
# backend/app/core/config.py

class Settings(BaseSettings):
    # Sheet 1: Work Status (기존)
    WORK_STATUS_SHEET_URL: str = "..."

    # Sheet 2: Hand Database - 제외됨

    # Sheet 3: Hand Analysis (신규)
    HAND_ANALYSIS_SHEET_URL: str = "https://docs.google.com/spreadsheets/d/1_RN_W_ZQclSZA0Iez6XniCXVtjkkd5HNZwiT6l-z6d4"
    HAND_ANALYSIS_SYNC_ENABLED: bool = False  # 기본 비활성화

    # 공통
    SHEETS_SYNC_INTERVAL_MINUTES: int = 30
```

---

## 6. 구현 범위

| 시트 | 상태 | 비고 |
|------|------|------|
| Sheet 1 (Work Status) | ✅ 완료 | 작업 현황 추적 |
| Sheet 2 (Hand Database) | ⏸️ 제외 | 타임코드 없음, 별도 시스템용 |
| **Sheet 3 (Hand Analysis)** | 🎯 구현 예정 | 타임코드 기반 진행률 추적 |

### 핵심 가치

**Sheet 3 (Hand Analysis) 구현 이유:**
- ✅ 타임코드 기반 진행률 추적 가능 (핵심 기능)
- ✅ NAS 파일과 매칭하여 완료 판정 자동화
- ✅ 8개 워크시트에 1,300+ 핸드 데이터 존재
- ✅ In/Out 타임코드로 `max(Out) ≥ 영상길이 × 0.9` 공식 적용

---

## 7. 다음 단계

### Sheet 3 (Hand Analysis) 구현 예정

| 단계 | 작업 | 예상 시간 |
|------|------|----------|
| 1 | `hand_analysis` 모델 생성 | 0.5일 |
| 2 | HandAnalysisSyncService 구현 | 1.5일 |
| 3 | NAS 파일 매칭 로직 | 1일 |
| 4 | 진행률 계산 서비스 | 0.5일 |
| 5 | API 엔드포인트 | 0.5일 |
| 6 | Frontend UI | 1일 |

**총 예상 시간: 5일**

---

## 부록: 기존 구현 참조

### A. sheets_sync.py 구조 (참조용)

```python
class SheetsSyncService:
    """Google Sheets 자동 동기화 서비스"""

    async def start(self):
        """스케줄러 시작"""

    async def sync(self) -> SyncResult:
        """동기화 실행"""

    async def _sync_to_db(self, db, records):
        """DB에 upsert"""

    def _normalize_header(self, header):
        """헤더 정규화"""

    def _normalize_status(self, status):
        """상태 정규화"""
```

### B. 타임코드 파싱 유틸리티

```python
def parse_timecode(timecode_str: str) -> float:
    """
    HH:MM:SS 또는 M:SS 형식을 초로 변환

    Examples:
        "6:58:55" → 25135.0
        "0:12:47" → 767.0
        "45:30" → 2730.0
    """
    parts = timecode_str.strip().split(':')

    if len(parts) == 3:
        h, m, s = map(float, parts)
        return h * 3600 + m * 60 + s
    elif len(parts) == 2:
        m, s = map(float, parts)
        return m * 60 + s
    else:
        return 0.0
```
