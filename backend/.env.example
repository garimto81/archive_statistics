# Application
APP_NAME=Archive Statistics Dashboard
APP_VERSION=1.0.0
DEBUG=true

# API
API_PREFIX=/api

# Database
DATABASE_URL=sqlite+aiosqlite:///./archive_stats.db

# NAS Connection
NAS_HOST=10.10.100.122
NAS_SHARE=docker
NAS_PATH=GGPNAs/ARCHIVE
NAS_USERNAME=GGP
NAS_PASSWORD=!@QW12qw

# Redis (optional - for background tasks)
REDIS_URL=redis://localhost:6379/0

# Email (optional - for alerts)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# CORS
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]
